# DevMap - an atlas of early mouse development

## Setup

First time:

```bash
mamba env create --file environment.yml
mamba activate devmap-paper
ipython kernel install --user --name devmap-paper
```

Update:

```bash
mamba env update --file environment.yml
```